import { StateManager } from "./StateManager";

export abstract class BaseState {

    EnterState(state: StateManager) {
        
    }

    UpdateState(state: StateManager, time: number) {
        
    }
}
